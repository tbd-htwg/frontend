# npm run dev:k8s — proxy /api/v2 and /api/search to the GKE Gateway (HTTPS).
# Requires k8s.tbd-htwg.de reachable (DNS, TLS). Firebase: set VITE_FIREBASE_* in .env.
VITE_API_BASE_URL=
VITE_DEV_API_PROXY_TARGET=https://k8s.tbd-htwg.de
