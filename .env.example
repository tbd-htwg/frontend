# API base URL (no trailing slash). Leave unset or empty for same-origin /v1
# (Vite dev proxy locally; Caddy -> backend container in infrastructure).
# Set to e.g. http://localhost:8080 only if you are not using the dev proxy.
# VITE_API_BASE_URL=
